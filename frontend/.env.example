# Frontend environment variables

# API URL configuration
# For Docker production: leave empty or don't set (uses /api nginx proxy)
# For local development with backend on different port: VITE_API_URL=http://localhost:8000
# VITE_API_URL=/api
